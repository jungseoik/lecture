# ── Dependencies ──
/node_modules
/.pnp
.pnp.*
.yarn/*

# ── Next.js ──
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# ── Production ──
/dist

# ── Env (로컬 비밀값은 커밋 금지, .env.example 만 커밋) ──
.env
.env*.local

# ── Deploy / tooling ──
.vercel
.netlify

# ── OMC (oh-my-claudecode) 상태 ──
.omc/
**/.omc/

# ── Supabase 로컬 ──
supabase/.temp
supabase/.branches

# ── Logs / OS ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem
/coverage
