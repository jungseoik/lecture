Summary tile pairing a pastel chip with a big blue figure. Used in the recommendation summary and list counters.

```jsx
<StatCard icon={<i className="ph-fill ph-graduation-cap" />} color="purple" label="추천 가능 대학" value="12" unit="개" />
```
