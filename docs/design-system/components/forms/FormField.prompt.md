Wraps any control with a label, optional `*` required mark, and helper or `error` text. The standard building block of every form grid.

```jsx
<FormField label="GPA" required hint="4.5 만점 기준">
  <Input placeholder="3.4" />
</FormField>
```
