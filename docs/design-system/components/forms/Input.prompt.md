Single-line text field. Pair with `FormField` for a label.

```jsx
<Input placeholder="대학명을 입력하세요" iconLeft={<i className="ph ph-magnifying-glass" />} />
```

`invalid` turns the border red; `size="lg"` for hero search bars; `iconLeft` for a leading glyph.
