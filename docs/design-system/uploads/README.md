# uploads/ (이동됨)

원래 이 폴더에 있던 기획 PDF·시안 PNG는 중복이라 제거했습니다.
**단일 출처는 [`docs/planning/`](../../planning/)** 입니다.

`docs/design-system/readme.md` 의 "출처 자료(Sources)" 항목이 가리키는 파일들은 모두 `docs/planning/` 에 있습니다.
